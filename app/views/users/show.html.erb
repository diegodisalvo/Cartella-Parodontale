<div class="content">
  <h1><%= current_user.email %></h1>
  <button class="btn btn-info"><%= link_to 'modifica', edit_user_path(current_user)%></button><button class="btn btn-success"><%= link_to 'Vai alla lista dei tuoi pazienti', :controller => 'patients', :action => 'index', :search => ''%></button>

  <hr />
  <h5>Nome e Cognome</h5>
  <p><% if current_user.first_name? %><%= current_user.first_name + ' ' + current_user.last_name %></p><% else %><p>non specificato</p><% end %>
  <h5>Professione</h5>
  <p><% if current_user.profession? %><%= current_user.profession %></p><% else %><p>non specificato</p><% end %>
</div>
